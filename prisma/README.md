# Prisma

Prisma setup is reserved for `[M1-T02]` and schema models are reserved for `[M1-T03]`.

This directory exists so follow-up milestone tasks can add `schema.prisma` and migrations without changing the project layout.
